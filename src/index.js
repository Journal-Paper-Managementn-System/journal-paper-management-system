import React from 'react';
import ReactDOM from 'react-dom/client';
import JPSub from './components/Submission';
import './components/Submission.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <JPSub />
    </React.StrictMode>
);